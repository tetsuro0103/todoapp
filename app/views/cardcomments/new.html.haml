.container
  %ul
    - @cardcomment.errors.full_messages.each do |message|
      %li= message

  = form_with(model: @cardcomment, url: task_cardcomments_path(@cardcomment.task), local: true) do |f|
    %div
      = f.label :content, 'New Comment'
    %div
      = f.text_area :content
    = f.submit '保存', class: 'btn-primary'